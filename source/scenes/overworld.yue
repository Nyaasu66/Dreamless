import "sti"
import "bump"
import "class.character" as Character

local map

{
    init: =>
        map = sti.load "$/data/map/overworld.lua"
    enter: =>
        Dialog.load "todo_gugugu"
    update: (dt) =>
        map::update dt
    draw: =>
        map::draw 0, 0, 2
}
